import requests
import pathlib
import templ8.programstate as ps

def neo():
	print("TODO: This function")
#	state = ps.ProgramState()
#	for subdir, dirs, files in os.walk(state.output_folder):
#		for file in files:
#			path = os.path.join(subdir, file)
#			from_root = os.path.normpath(path.replace(state.output_folder, "", 1))
#			print(from_root)
